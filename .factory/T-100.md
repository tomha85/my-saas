# T-100 (Bootstrap Flask App)

## Tasks
1. Create the project structure:
   - Create `app/__init__.py`
   - Create `app/main.py`

2. Implement the Flask app:
   - The route `GET /health` should return JSON `{"status":"ok"}`.

3. Create `requirements.txt`:
   - Include `Flask` pinned version.
   - Include `pytest` for tests.

4. Create tests:
   - Implement `tests/test_health.py` using Flask test client.

5. Write a `README.md`: 
   - Include exact steps to create a virtual environment, install dependencies, run the app, and test it.

6. Update `.factory/STATUS.md` with completed tasks.

## Branch
- `feature/t-100-flask-bootstrap`

## Workflow
- Ensure the Deployer opens a PR to the main branch and replies with the PR URL.